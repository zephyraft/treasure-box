# Treasure-Box

Recipes of Java Programming

## 依赖版本定义

依赖版本统一定义在gradle.properties

## 常用脚本

根目录执行`just`查看

### lfs

```shell
git lfs track "*.png"
git lfs track "*.svg"
git lfs track "*.jpg"
git lfs track "*.xmind"
git lfs track "*.pdf"
```
