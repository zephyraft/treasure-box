debug: false
server:
  port: 8080
spring:
  profiles:
    active: dev
  application:
    name: app-demo-prometheus
management:
  endpoints:
    web:
      exposure:
        include: prometheus
      base-path: /actuator
